apiVersion: v1
kind: Service
metadata:
  name: {{ include "concept.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "concept.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "concept.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
