replicaCount: 2

image:
  repository: ghcr.io/paulodhiambo/concept
  pullPolicy: IfNotPresent
  tag: b8a65ee

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

istio:
  enabled: false
  host: concept.example.com
  gateway: istio-system/ingressgateway
  retries:
    attempts: 3
    perTryTimeout: 5s

ingress:
  enabled: true
  baseDomain: cluster.lakesideinn.space
  auth:
    enabled: true

database:
  configMap:
    create: false
    name: "concept-db-config"
    host: localhost
    port: "5432"
    dbName: conceptdb
  secret:
    create: false      # set true to let Helm create the secret; prefer external management (Vault, SealedSecrets)
    name: "concept-db-secret"
    usernameKey: username
    passwordKey: password
    username: ""       # only used when create: true
    password: ""       # only used when create: true

probes:
  liveness:
    path: /actuator/health/liveness
    initialDelaySeconds: 60
    periodSeconds: 10
    failureThreshold: 5
  readiness:
    path: /actuator/health/readiness
    initialDelaySeconds: 45
    periodSeconds: 10
    failureThreshold: 5
