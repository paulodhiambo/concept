spring:
  application:
    name: concept

management:
  health:
    probes:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
